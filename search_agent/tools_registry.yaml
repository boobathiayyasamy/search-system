# Tools Registry
# Defines which tools to register and in what order

version: "1.0"

tools:
  - name: time_tool
    module: search_agent.tools.mcp
    function: create_mcp_toolset
    enabled: true
    order: 1
